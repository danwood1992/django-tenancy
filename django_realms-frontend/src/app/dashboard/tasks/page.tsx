import { type Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Dashboard',
}

export default function Account() {
  return (
      <div>
      <h1>Tasks section</h1>
      </div>
  )
}